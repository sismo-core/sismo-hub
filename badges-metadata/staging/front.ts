import { frontBadges as frontBadgesMain } from "@badges-metadata/main/front";
import { BadgesCollection } from "topics/badge";

export const frontBadges: BadgesCollection = {
  ...frontBadgesMain,
};
