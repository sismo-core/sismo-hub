import { BadgeAPIType } from "../../badge/api";

/**
 * @description An object that represents the AttestationsCollection Object for the end user using the api.
 */
export type AttestationsCollectionAPIType = {
  badge: BadgeAPIType;
};
